# 默认 HostLink / 微后端依赖，与 wheel 的 Requires-Dist 共用此清单。
# 硬件专用 SDK 见 unilabos[drivers] 或设备包自己的依赖声明。
setuptools>=68
packaging>=24
PyYAML>=6
pydantic>=2.11,<3
numpy>=2,<3
networkx>=3.2
typing_extensions>=4.12
websockets>=12
msgcenterpy>=0.1.8
orjson>=3.11
pint>=0.23
fastapi>=0.115
sqlmodel>=0.0.39,<0.1
jinja2>=3.1.4
requests>=2.32
httpx>=0.27.0
mcp>=1.30,<2
uvicorn>=0.30
pyserial>=3.5
crcmod-plus>=2.3.1
pymodbus>=3.6.9,<4
# 修正版随发行包的 wheelhouse 分发，不上传 PyPI；安装时须提供 --find-links。
# 本地版本号区分 UniLabOS fork 与同版本上游，且不包含离线时仍会联网的源码 URL。
pylabrobot==0.2.2+unilabos.6285d662effa
opentrons-shared-data==9.1.0+unilabos.np2.1
