package:
  name: ros-humble-unilabos-msgs
  version: 0.12.0

source:
  path: ../../unilabos_msgs
  target_directory: src

build:
  number: 2
  script:
  - if: win
    then:
    - copy %RECIPE_DIR%\bld_ament_cmake.bat %SRC_DIR%
    - call %SRC_DIR%\bld_ament_cmake.bat
  - if: unix
    then:
    - cp $RECIPE_DIR/build_ament_cmake.sh $SRC_DIR
    - bash $SRC_DIR/build_ament_cmake.sh

about:
  repository: https://github.com/deepmodeling/Uni-Lab-OS
  license: BSD-3-Clause
  description: "ros-humble-unilabos-msgs provides the ROS 2 Humble message definitions for Uni-Lab-OS."

requirements:
  build:
  - ${{ compiler('cxx') }}
  - ${{ compiler('c') }}
  - python ==3.12.13
  # Humble 的 Windows rosidl_adapter 仍使用 EmPy 3 API；与 ci-check.yml 保持一致。
  - empy ==3.3.4
  - numpy >=2,<3
  - if: build_platform != target_platform
    then:
    - pkg-config
    - cross-python_${{ target_platform }}
  - if: linux and x86_64
    then:
    - sysroot_linux-64 ==2.17
  - ninja
  - setuptools
  - cython
  - cmake
  - if: unix
    then:
    - make
    - coreutils
  - if: osx
    then:
    - tapi
  - if: win
    then:
    - vs2022_win-64
  host:
  - python ==3.12.13
  - empy ==3.3.4
  - python_abi 3.12.* *_cp312
  - numpy >=2,<3
  - pip
  - if: build_platform == target_platform
    then:
    - pkg-config
  - robostack-humble::ros-humble-action-msgs
  - robostack-humble::ros-humble-ament-cmake
  - robostack-humble::ros-humble-ament-lint-auto
  - robostack-humble::ros-humble-ament-lint-common
  - robostack-humble::ros-humble-ros-environment
  - robostack-humble::ros-humble-ros-workspace
  - robostack-humble::ros-humble-rosidl-default-generators
  - robostack-humble::ros-humble-std-msgs
  - robostack-humble::ros-humble-geometry-msgs
  - robostack-humble::ros2-distro-mutex 0.9.* humble_*
  run:
  - python >=3.12,<3.13
  - python_abi 3.12.* *_cp312
  - numpy >=2,<3
  - robostack-humble::ros-humble-action-msgs
  - robostack-humble::ros-humble-ros-workspace
  - robostack-humble::ros-humble-rosidl-default-runtime
  - robostack-humble::ros-humble-std-msgs
  - robostack-humble::ros-humble-geometry-msgs
  - robostack-humble::ros2-distro-mutex 0.9.* humble_*
  - if: osx and x86_64
    then:
    - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
