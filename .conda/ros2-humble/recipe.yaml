# 显式选装；CLI 仍需 --backend ros2。
# channel 在安装/构建命令中指定，不能写入成品包的运行依赖。
package:
  name: unilabos-ros2
  version: 0.12.3
build:
  noarch: generic
  number: 0
  string: humble_0
requirements:
  run:
    - unilabos ==0.12.3 py312_0
    - python ==3.12.13
    - opencv
    - transforms3d
    - ros2-distro-mutex 0.9.* humble_*
    - ros-humble-ros-core
    - ros-humble-action-msgs
    - ros-humble-std-msgs
    - ros-humble-geometry-msgs
    - ros-humble-control-msgs
    - ros-humble-nav2-msgs
    - ros-humble-cv-bridge
    - ros-humble-vision-opencv
    - ros-humble-tf-transformations
    - ros-humble-moveit-msgs
    - ros-humble-tf2-ros
    - ros-humble-tf2-ros-py
    - ros-humble-unilabos-msgs ==0.12.1
about:
  license: GPL-3.0-only
  description: Optional UniLabOS ROS 2 humble transport (CPython 3.12)
