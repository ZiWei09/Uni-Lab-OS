# unilabos-full: Full package with all features
# Depends on unilabos + complete ROS2 desktop + dev tools

package:
  name: unilabos-full
  version: 0.12.2

build:
  noarch: generic
  number: 0
  string: jazzy_0

requirements:
  run:
  # Base unilabos package (includes unilabos-env)
  - unilabos ==0.12.2 jazzy_0
  # Documentation tools
  - sphinx
  - sphinx_rtd_theme
  # Web UI
  - gradio
  - flask
  # Interactive development
  - ipython
  - jupyter
  - jupyros
  - colcon-common-extensions
  # ROS2 full desktop (includes rviz2, gazebo, etc.)
  - robostack-jazzy::ros-jazzy-desktop-full
  # Navigation and motion control
  - robostack-jazzy::ros-jazzy-navigation2
  - robostack-jazzy::ros-jazzy-ros2-control
  - robostack-jazzy::ros-jazzy-robot-state-publisher
  - robostack-jazzy::ros-jazzy-joint-state-publisher
  # MoveIt motion planning
  - robostack-jazzy::ros-jazzy-moveit
  - robostack-jazzy::ros-jazzy-moveit-servo
  # Simulation
  - robostack-jazzy::ros-jazzy-simulation

about:
  repository: https://github.com/deepmodeling/Uni-Lab-OS
  license: GPL-3.0-only
  description: "UniLabOS Full - ROS 2 Jazzy Desktop, MoveIt, Navigation2, Gazebo, and Jupyter"
