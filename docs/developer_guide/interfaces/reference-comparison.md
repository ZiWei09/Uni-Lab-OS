# 与原《设备接入与贡献规范》的对照

参照文档为 [UniLabOS 设备接入与贡献规范、OS 配合模式](https://dptechnology.feishu.cn/docx/YRZtdUKkJo1zu4xgjohcc498neh)，
本次读取版本 revision 562。原文兼有接入说明、阶段性实现和后续规划；本章不修改原文，
只说明哪些理念保留、哪些表述需要随当前 dev 更新。飞书访问权限独立于本仓库。

## 比较基线

前一次对比以 `2176e776`（首次 Jazzy 提交的父提交）为起点、`c235e971`（2026-09-06）为终点；
这样会把首次 Jazzy 迁移本身算入变更。本手册迁移统计从 `99558a5a` 开始，终点延伸至
`862ce87a38222ac2d51739f2f89e5daecd3cd907`（2026-09-14）。范围不同，不能直接比较文件数或接口数量。

原对比中的 OpenAPI 为 158 个操作；当前离线全集为 163 个操作，新增内容及实际合同以
[API 全目录](api-catalog.md) 为准。数量增加本身不证明实现完整，例如 handler 的动态状态码和
未声明具体 DTO 的响应仍需人工审阅。旧报告中的六个 demo 也不能替代当前七个 demo 的验收入口。

## 保留的理念

设备能力优先用声明表达，让类型、默认值和说明复用于界面及工作流；降低驱动接入门槛，
让人工与 AI 编写的代码遵守同一合同；设备包独立分发；在连真机前使用可复现的仿真和测试。
这些目标与本轮设计一致，不需要因底层从单一 ROS 走向多 backend 而放弃。

需要补充的是：自动生成参数表单不替代用户确认，仿真成功不替代真机安全验收，
实验语义的组合不替代单个物理动作的状态和历史记录。

## 需要更新的表述

| 原文主题 / 旧表述 | 当前应如何说明 | 详细依据 |
| --- | --- | --- |
| Python 3.10+ 的接入环境 | 本版以 Python 3.12 为运行基线；ROS2 Jazzy/Humble 依赖环境分开说明，HostLink 无需 ROS 通信运行时 | [迁移](migration.md) |
| `displayname` 与 `display_name` 的示例/兼容说法 | 当前装饰器签名使用 `display_name`，不能继续给新驱动推荐旧 keyword | [Python 接入](python-cli-mcp.md) |
| 设备能力声明自动变成 ROS 接口 | 声明交给共享执行内核，按 HostLink 或 ROS2 初始化；特殊 ROS 能力不能宣称通用支持 | [设计](design.md) |
| DDS 负责订阅的发现与重连 | 这只适用于 ROS2 对应链路；HostLink、控制 WS 和前端 SSE 各有自己的连接生命周期 | [实时通道](realtime.md) |
| Edge 收到决定后自行 retry/skip/recovery | Host 等待后端决策并放行原尝试；后端处理 failed 及新的 retry job，保留关联历史 | [异常决策](workflows.md) |
| error_policy 的 allow_retry、allow_skip、default_on_user_timeout 等形状 | 当前 options 按异常类映射，使用 max_retries、decision_timeout_seconds、default_on_decision_timeout；不能把原示例当当前请求 DTO | [策略字段](python-cli-mcp.md) |
| timeout/execution_timeout 尚属设计或统一处理 | 本版已有硬/软超时及软超时表达式；wait 与重新执行不同，超时不保证物理动作停止 | [工作流](workflows.md) |
| 锁主要由设备报告闲忙 | 动作准入结合在线能力、动作/物料锁声明与库存需求；嵌套作用域锁仍不是本版能力 | [设计](design.md) |
| 物料作为 Edge 本地树，同步到平台 | 权威分配实例身份；本地是投影；位置/内容分开；transfer 要核对权威和两端加载状态 | [物料](materials.md) |
| 模板默认参数可直接提交 | Site 标签默认值是待确认绑定；浏览器 preserve，API resolve 唯一身份，普通字符串不自动改写 | [Site 规则](workflows.md) |
| 一个执行消息同时承担运行与观察 | 控制 WS 只供 Host；前端用 SSE 提醒加 HTTP 拉取，不共享执行控制槽位 | [实时通道](realtime.md) |
| `device_pair.yaml`、`--sim_engine` 的使用示例 | 保留真机/仿真双验证的目标；当前 CLI 没有 `--sim_engine`，不能据旧文档承诺该开关仍可用 | [CLI](python-cli-mcp.md) |
| 注册表扫描加速倍数与设备/接口总数 | 属于当时环境的数据，不能原样当成本版全局指标；按当前安装包、缓存状态与实际测试报告给出 | [注册表](http.md) |
| 平台统一登录、DataCore、前端广场等配合模式 | 区分平台规划和微后端实际 API；本版普通 HTTP 没有统一认证，前端独立，不把合作方案写成已实现安全能力 | [HTTP 边界](http.md) |
| 历史提交/文档中的字段已加入即可视为可用 | 逐项核对 handler、协议、调度消费与测试；例如注册表支持与 endpoint 在线不是同一状态 | [设计](design.md) |

## 两种文档服务不同读者

面向用户的说明应先回答：怎样接入设备、怎样绑定物料并运行、遇到错误怎样处理、在哪看进度和日志。
正文不需要列出全部表名、消息模型和 163 条路由，但要说清边界，不承诺尚未实现的能力。

开发者手册则必须保留准确的 method/path、请求模型、响应信封、状态码、版本与幂等语义，
并给出跨进程失败、Site 歧义、人工替换和日志恢复的处理方式。
两者共享同一个源码基线，不能让简版沿用旧策略、详细版采用新策略。

## 本次对比没有做什么

没有重新下载原文附件、修改原飞书文档或发布新在线文档；没有把历史报告的测试结果当作本次运行结果；
没有合入工作区未提交代码；也没有改动 Legacy 的旧后端兼容实现。
现有实现的已知缺口集中在 [迁移与限制](migration.md)，不在说明文档中替代码补齐。
