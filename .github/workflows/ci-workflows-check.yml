name: CI Workflow Contracts

on:
  pull_request:
    branches: [main, dev]
    paths: ['.github/workflows/**', '.github/tests/**', '.conda/**', 'recipes/**', 'scripts/*release.py', 'setup.py', 'unilabos/__init__.py', 'unilabos/utils/requirements*.txt']
  push:
    branches: [main, dev]
    paths: ['.github/workflows/**', '.github/tests/**', '.conda/**', 'recipes/**', 'scripts/*release.py', 'setup.py', 'unilabos/__init__.py', 'unilabos/utils/requirements*.txt']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  workflow-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: false
      - name: Validate workflow syntax
        run: go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 -shellcheck='' -pyflakes='' .github/workflows/ci-check.yml .github/workflows/multi-platform-build.yml .github/workflows/unilabos-conda-build.yml .github/workflows/conda-pack-build.yml .github/workflows/wheel-release.yml .github/workflows/ci-workflows-check.yml .github/workflows/deploy-docs.yml
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Test build provenance and no-op handling
        run: |
          python -m pip install PyYAML==6.0.3
          python -m unittest discover -s .github/tests -v
